[project]
name = "inception-export"
version = "0.5.2"
description = "Export INCEpTION annotations into per-document/annotator XMI ZIPs"
requires-python = "~=3.9.0"
dependencies = [
    "pycaprio",
    "PyInquirer",
]
